{
  "name": "hf2-customer-distance-service",
  "version": "1.0.0",
  "private": true,
  "description": "Small offline REST service over Postgres: idempotent seed/geocode, /customers/count, /customers/by-distance",
  "type": "commonjs",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "migrate": "node --env-file-if-exists=.env node_modules/.bin/node-pg-migrate up",
    "seed": "node --env-file-if-exists=.env scripts/seed.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --env-file-if-exists=.env --test"
  },
  "dependencies": {
    "express": "5.2.1",
    "node-pg-migrate": "8.0.4",
    "pg": "8.22.0"
  }
}
